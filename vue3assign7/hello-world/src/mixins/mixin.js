// create an event listener for the image if clicked, add a border if clicked. Vanilla javascript mixin add border, don't use mixin in search.
// toggle class image border, 
// also, create a function that creates a toggle for the border,
// for function, name must match

function toggleBorder(){ //eslint-disable-line no-unused-vars
    alert("toggleBorder")


}